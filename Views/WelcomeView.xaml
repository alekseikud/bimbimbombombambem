<UserControl x:Class="WpfApp3.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource SurfaceBrush}">
        <StackPanel Width="360" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="/Resources/vinyl.png" Width="86" Height="86" Margin="0,0,0,18" />
            <TextBlock Text="Musica"
                       FontSize="34"
                       FontWeight="Bold"
                       Foreground="{StaticResource DarkTextBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Choose a library to start managing songs and playlists."
                       TextWrapping="Wrap"
                       Foreground="#596273"
                       FontSize="14"
                       TextAlignment="Center"
                       Margin="0,8,0,26" />
            <Button Content="Create New Library"
                    Command="{Binding CreateLibraryCommand}"
                    Style="{StaticResource AccentButtonStyle}"
                    Height="38"
                    Margin="0,0,0,10" />
            <Button Content="Open Existing Library"
                    Command="{Binding OpenLibraryCommand}"
                    Style="{StaticResource QuietButtonStyle}"
                    Height="38" />
        </StackPanel>
    </Grid>
</UserControl>
